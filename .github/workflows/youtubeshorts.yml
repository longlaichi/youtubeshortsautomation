<<<<<<< HEAD
name: Instagram Reel Uploader

on:
  schedule:
    # Runs every 4 hours = 6 times per day
    - cron: "0 */4 * * *"
  workflow_dispatch: # allow manual trigger

jobs:
  run-bot:
=======
name: ytUploader

on:
  schedule:
    # Runs every day at 5:00 AM IST (23:30 UTC previous day)
    - cron: "30 23 * * *"
  workflow_dispatch: # manual trigger

jobs:
  upload:
>>>>>>> f41f913 (Save local changes before rebase)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Restore token.pickle
<<<<<<< HEAD
        run: |
          echo "${{ secrets.GDRIVE_TOKEN_PICKLE }}" | base64 --decode > token.pickle

      - name: Run main.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          IG_USERNAME: ${{ secrets.IG_USERNAME }}
          IG_PASSWORD: ${{ secrets.IG_PASSWORD }}
        run: |
          python main.py
=======
        run: |
          echo "${{ secrets.GDRIVE_TOKEN_PICKLE }}" | base64 --decode > token.pickle

      - name: Run main.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          IG_USERNAME: ${{ secrets.IG_USERNAME }}
          IG_PASSWORD: ${{ secrets.IG_PASSWORD }}
        run: python main.py

      - name: Commit posted.json (if changed)
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add posted.json
            git commit -m "Update posted log [skip ci]"
            git push
          else
            echo "No changes to commit."
          fi
>>>>>>> f41f913 (Save local changes before rebase)
